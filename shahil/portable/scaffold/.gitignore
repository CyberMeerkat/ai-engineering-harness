# Local settings (machine-specific permissions, may contain tokens in allow rules)
settings.local.json

# Mutable state (session-local, not shared via git)
state/

# Context continuation snapshots (ephemeral, session-scoped)
compact/

# OS artifacts
.DS_Store
